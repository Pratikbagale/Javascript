// Q.For a give array of numbers,print the square of each value using the forEach loop..

let numbers = [2, 3, 4, 5, 6];

numbers.forEach((numbers) => {
    console.log(numbers * numbers);// num**2
});