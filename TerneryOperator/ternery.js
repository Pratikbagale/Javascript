// // Ternery Operator
// // condition ? true output : false output

// let age = 54;

// //age >= 18 ? "adult" : "not adult";

// //let result = age >= 18 ? "adult" : "not adult";

// //console.log(result);

// age >= 18 ? console.log("adult") : console.log("not adult");

let age = 20;
age >= 21 ? "Beer" : "Juice";

let result = age >= 21 ? "Beer" : "Juice";
console.log(age); // "Beer"
console.log(result);
